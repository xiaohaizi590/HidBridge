# 蓝牙 HID 方案说明

> 方案来源：从 Bluke 项目（[arnav-kr/Bluke](https://github.com/arnav-kr/Bluke)）提取核心代码，整理为本项目可独立运行的最小实现。

## 1. 方案概述

**目标**：让 Android 手机通过内置蓝牙，直接向电脑（Windows / macOS / Linux / ChromeOS / Android TV / 游戏主机）发送符合 HID 标准的信号。对电脑而言，手机和一只普通的蓝牙键盘/鼠标/手柄没有区别。

**核心思路**：利用 Android 9+ 提供的 `BluetoothHidDevice` API（HID-over-GATT / BR-EDR HID Device 角色），手机把自己**注册成一台 HID 设备**，而不是去连接别人。

```
┌──────────┐   蓝牙 HID 链路（手机为 HID 设备，电脑为主机）   ┌──────────┐
│  Android  │ ◄───────────────────── L2CAP/HID ──────────────────► │  电脑     │
│   手机    │  sendReport(键盘/鼠标/手柄报表)                       │ 无需驱动  │
└──────────┘                                                       └──────────┘
```

## 2. HID 概念

- **HID**（Human Interface Device，人机交互设备）：键盘、鼠标、游戏手柄都属于此类。
- **HID Report Descriptor（报表描述符）**：设备启动时上交给主机的"说明书"，声明设备包含哪些报表（Report）、每个报表的字段布局、取值范围。
- **HID Report（报表）**：设备按键/移动时上报的数据包，格式与描述符一一对应。
- **蓝牙 HID**：HID 通过蓝牙传输，支持 HID Device 角色的一端把按键数据发往主机端，主机无需安装驱动。

## 3. 系统架构

本项目只有 3 个核心代码文件：

| 文件 | 职责 |
|---|---|
| `bluetooth/BluetoothKeyboardManager.kt` | **核心引擎**：HID 注册、配对、连接、三种报表发送、LED 状态回读 |
| `ui/HidScreen.kt` | 最小验证 UI：状态显示、设备连接、键盘/鼠标/手柄测试 |
| `MainActivity.kt` | 入口：动态权限申请、引擎初始化 |

### 核心引擎状态机

```
Unsupported ──► PermissionRequired ──► BluetoothOff ──► ProfileNotSupported
                                                          │
                          ReadyDisconnected ◄──► PairingMode ◄──► Connected
```

引擎用 `StateFlow` 对外暴露状态：
- `serviceState: StateFlow<BluetoothState>`
- `statusMessage: StateFlow<String>`
- `bondedDevices / scannedDevices / isScanning`
- `connectedDevice`
- `capsLockState / numLockState / scrollLockState`（主机 LED 回读）

## 4. 三种 HID 报表

见 `BluetoothKeyboardManager.hidDescriptor`，设备一次性声明为 **键盘 + 鼠标 + 手柄 组合设备**（SDP 子类 `SUBCLASS1_COMBO`）。

### Report ID 1 —— 键盘（8 字节）

| 字节 | 含义 |
|---|---|
| 0 | 修饰键位图：bit0=LCtrl(0xE0) bit1=LShift(0xE1) bit2=LAlt(0xE2) bit3=LGui(0xE3) bit4=RCtrl bit5=RShift bit6=RAlt bit7=RGui |
| 1 | 保留位（恒 0x00） |
| 2-7 | 6 键无冲突（NKRO 受限），值为 HID Usage ID |

发送接口：`sendKey(keyCode: Int, isPress: Boolean)`，keyCode 为 HID Usage ID（如 `A`=0x04，Enter=0x28，修饰键 0xE0-0xE7）。

### Report ID 2 —— 鼠标 / 触控板（4 字节）

| 字节 | 含义 |
|---|---|
| 0 | 按键位图：bit0=左键 bit1=右键 bit2=中键 |
| 1 | X 相对位移（-127..127） |
| 2 | Y 相对位移（-127..127） |
| 3 | 滚轮（-127..127） |

发送接口：`sendMouseReport(buttons, x, y, wheel)`。

### Report ID 3 —— 手柄（11 字节）

| 字节 | 含义 |
|---|---|
| 0-2 | 18 个按键位图 |
| 3-4 | 左摇杆 X（16bit 无符号，中心 32768，小端） |
| 5-6 | 左摇杆 Y |
| 7-8 | 右摇杆 X |
| 9-10 | 右摇杆 Y |

发送接口：`sendGamepadReport(buttonMask, leftX, leftY, rightX, rightY)`，轴输入归一化 `-1.0f..1.0f`。

## 5. 关键流程

### 5.1 初始化与注册（把手机变成 HID 设备）

```
checkBluetoothCapabilities()
  └─ initProfileListener()          // 异步获取 HID_DEVICE 代理（BluetoothProfile.HID_DEVICE = 19）
       └─ onServiceConnected()      // 代理绑定成功
            └─ registerApp()        // 注册 HID 应用
                 ├─ unregisterApp() // 先清理系统残留状态
                 ├─ delay(300ms)    // 硬件防抖：防止蓝牙守护进程 IPC 崩溃
                 └─ hid.registerApp(sdpSettings, null, null, executor, hidCallback)
```

关键点：
- **SDP 设置**：设备名、描述（"Wireless Controller Combo"）、子类 `SUBCLASS1_COMBO`、HID 描述符。
- **注册前必须 `unregisterApp()` + 300ms 延迟**，否则 `com.android.bluetooth` 守护进程可能崩溃（DeadObjectException）。
- **反射伪装设备类别**：`spoofLocalDeviceClass()` 调用隐藏 API `setBluetoothClass(0x000005C0)`，让电脑识别为"组合外设"。

### 5.2 配对与连接

- `pairDevice()`：对未配对设备调用 `createBond()`，配对成功后自动连接。
- `connectDevice()`：等待代理就绪 → 若已连接其他设备则重启 HID 服务切换 → `hid.connect(device)` → 失败重试一次。
- 连接/断开/配对状态全部通过 `BroadcastReceiver` + `onConnectionStateChanged` 回调更新。

### 5.3 发送按键（核心路径）

```
UI 输入 ──► sendKey(usageId, isPress)          // 维护 activeModifiers + activeKeys(6键)
            └─► 组装 8 字节键盘报表
                 └─► submitReport(dev, 1, report)
                      └─► reportExecutor(前台优先级线程)
                           └─► hid.sendReport(dev, 1, report)   // 发送给电脑
```

### 5.4 主机 LED 状态回读

主机（如电脑）可能发送 LED 状态（CapsLock/NumLock/ScrollLock）：
- `onSetReport` / `onInterruptData` 回调收到 Report ID 1 的 Output 报表
- `parseLedReport()` 解析并更新 `capsLockState` 等 StateFlow

## 6. 硬件前提与限制

- **Android 9（API 28）+**：`BluetoothHidDevice` 由此版本引入（代码里有显式校验）。
- **芯片组支持 HID Device 角色**：`getProfileProxy(HID_DEVICE)` 失败即 `ProfileNotSupported`，能否使用取决于手机蓝牙栈/厂商 ROM。
- **同一时刻一个蓝牙连接**：手机 HID Device 角色与"作为主机连手柄/耳机"互斥（射频同通道），连接 HID 后 A2DP/Headset 音频链路会被主动断开（`disconnectAudioProfiles`）。
- **无需 root、无需宿主软件**：纯系统 API。

## 7. 扩展点

| 方向 | 说明 | 位置 |
|---|---|---|
| 外部手柄输入（USB-C 拉伸手柄） | 重写 `dispatchGenericMotionEvent`/`dispatchKeyEvent` 读取物理手柄，映射后喂给 `sendGamepadReport`。注意：**蓝牙拉伸手柄不可行**（与 HID Device 角色冲突），必须走 USB-C | MainActivity / 新增 InputBridge |
| 机械键盘音效 | 监听按键事件播放按键音 | 新增 SoundEngine |
| 键盘布局/主题 | UI 层自定义 | HidScreen |
| 分时上报优化 | `sendGamepadReport` 已内置 8ms 上报节流，可按需调整 | HidScreen |

## 8. 本文件与 Bluke 原版的差异（提取时做的改动）

1. 包名 `dev.arnv.bluke` → `dev.hid.demo`。
2. 移除 `DeveloperLogManager` 日志依赖 → 直接使用 `Log.d/e`。
3. 移除 `R.string.app_name` 资源引用 → 设备名用字面量。
4. 移除音效、主题、完整键盘 UI、设置页等与"蓝牙 HID 方案"无关的代码。
5. 核心引擎 `BluetoothKeyboardManager.kt` 逻辑 100% 保留。
