# 部署与使用说明

> 版本：v2.0 | 更新：2026-08-06
> 基于蓝牙 HID + WiFi UDP 桥接方案，实现手机拉伸手柄到电脑的低延迟游戏体验。

---

## 一、你需要准备什么

### 硬件

| 项目 | 要求 |
|------|------|
| 手机 | Android 9+（API 28+），蓝牙支持 HID Device 角色 |
| 电脑 | Windows 10/11，蓝牙适配器，支持 ViGEmBus |
| 拉伸手柄 | USB-C 连接手机（蓝牙手柄会与 HID 冲突） |
| WiFi | 手机和电脑在同一局域网（用于 UDP 数据通道） |

### 软件

| 项目 | 说明 |
|------|------|
| 手机 App | `BluetoothHID-Demo` 安装包（APK） |
| PC 接收器 | `receiver.exe`（C 编译，已内置 ViGEmClient） |

---

## 二、PC 端安装（首次）

### 2.1 接收安装包

1. 手机 App 点 **"推送安装包到电脑"**
2. 系统分享面板选择 **"蓝牙"**
3. 选择你的电脑设备
4. 电脑蓝牙通知 → 点 **"保存"**，文件默认存到 `C:\Users\<用户名>\Documents`

### 2.2 安装驱动

1. 进入文件保存目录，找到 `receiver.exe`
2. **右键 → 以管理员身份运行**（或右键菜单 → "以管理员身份运行"）
3. 第一次运行会弹出 UAC 提示 → 点 **"是"**
4. 程序自动完成：
   - 安装 ViGEmBus 虚拟手柄驱动
   - 复制自身到 `%LOCALAPPDATA%\HidReceiver\`
   - 注册开机自启
   - 显示托盘图标（右下角）

### 2.3 验证安装

托盘图标出现 = 安装成功。程序在后台运行，开机自动启动。

---

## 三、手机 App 安装

### 3.1 编译构建

```bash
# 克隆项目后
./gradlew assembleDebug
# 或用 Android Studio 打开项目，Build → Build APK
```

### 3.2 安装

1. 手机开启 **USB 调试**（开发者选项中）
2. USB 连接电脑
3. `adb install app-debug.apk`
4. 或直接把 APK 拷到手机点击安装

### 3.3 权限授予

首次打开 App，需要授予以下权限：
- **蓝牙**（BLUETOOTH_CONNECT）
- **位置**（ACCESS_FINE_LOCATION）
- **附近的设备**（NEARBY_WIFI_DEVICES）

---

## 四、蓝牙连接流程

### 4.1 手机注册为 HID 设备

打开 App → 自动初始化为"蓝牙键盘+鼠标+手柄组合设备"
- 设备名：`BluetoothHID-Demo`
- 状态：`就绪，等待连接`

### 4.2 配对与连接

**方式 A：手机扫描电脑（推荐）**

1. 电脑蓝牙设置 → 点 **"添加设备"** → 选择 **"蓝牙"** → 让电脑可被发现
2. 手机 App → 点 **"扫描设备"**
3. 扫描到你的电脑 → 点选
4. 两边弹出配对码 → 都点 **"确认"**
5. 状态变为 **"已连接"**

**方式 B：电脑主动连手机**

1. 电脑蓝牙设置 → 点 **"添加设备"**
2. 选择你的手机 → 配对码确认
3. 状态变为 **"已连接"**

### 4.3 验证连接

- 电脑"控制面板 → 游戏控制器"中出现 `Bluetooth HID Device`
- 手机 App 状态显示 **"已连接"**
- 手机按按键 → 电脑端设备测试面板有反应 ✅

---

## 五、WiFi 桥接（可选，提升延迟）

### 5.1 确认前置条件

- 手机和电脑连接 **同一个 WiFi**
- PC 端 `receiver.exe` 已在后台运行（托盘图标可见）

### 5.2 开启 WiFi 桥接

1. 手机 App → **"WiFi 桥接"** 卡片
2. IP 地址自动发现（或手动输入 PC IP）
3. 打开顶部开关 → **成功**

### 5.3 验证桥接

- 电脑托盘图标变绿/蓝色
- 手机 App 显示 **"桥接就绪"**
- 手柄数据通过 WiFi 传输，延迟更低

### 5.4 回报率设置

在桥接卡片底部选择回报率：
- **省电 125Hz** → 蓝牙默认，省点
- **均衡 250Hz** → 日常使用
- **电竞 500Hz** → WiFi 默认，低延迟
- **极致 750Hz** → 电竞专用

---

## 六、使用拉伸手柄

### 6.1 连接手柄

1. USB-C 线连接拉伸手柄到手机
2. 手机弹出"允许 USB 调试？" → **允许**
3. App 自动识别手柄 → 状态显示 **"手柄已连接"**

### 6.2 开始游戏

1. 手机 App 保持在前台
2. 打开手机上的游戏（拉伸手柄已映射为 HID）
3. 同时电脑上的虚拟手柄也同步接收数据
4. 可用 **Screen Cast** 或 **Scrcpy** 投屏到电脑玩

### 6.3 双模式说明

| 模式 | 数据路径 | 延迟 | 适用场景 |
|------|----------|------|----------|
| 纯蓝牙 HID | 手机→蓝牙→电脑 | 20-50ms | 省电，无线自由 |
| WiFi 桥接 | 手机→WiFi UDP→电脑 | 2-5ms | 电竞，低延迟 |
| 混合模式 | 手机同时发蓝牙+WiFi | - | 自动切换 |

---

## 七、常见问题

### Q1: 手机扫描不到电脑？

1. 确认电脑蓝牙已开启且"可被发现"
2. 确认手机蓝牙权限已授予
3. 重启手机蓝牙和电脑蓝牙

### Q2: 配对成功但连接失败？

1. 取消配对 → 重新配对
2. 电脑"设备管理器 → 蓝牙 → 移除设备" → 重新配对
3. 确认手机 Android 版本 ≥ 9

### Q3: 电脑没识别到手柄？

1. 检查"控制面板 → 游戏控制器"
2. 如果没显示，手机 App 断开 → 重新连接
3. 重启电脑蓝牙服务

### Q4: WiFi 桥接连不上？

1. 确认手机和电脑在同一 WiFi
2. 确认 PC 端 `receiver.exe` 托盘图标可见
3. 尝试关闭防火墙或添加例外
4. 手机 App → WiFi 桥接 → 手动输入 PC IP

### Q5: receiver.exe 托盘图标消失？

程序可能已停止。重启方式：
1. 运行 `%LOCALAPPDATA%\HidReceiver\receiver.exe`
2. 或开机自动启动（安装后已注册）

### Q6: 驱动安装失败？

1. 确保以管理员身份运行
2. 下载最新版 ViGEmBus_Setup.exe 手动安装
3. 检查 Windows 版本（需 Win10 1809+ 或 Win11）

### Q7: 手柄按键卡顿？

1. 切换到 WiFi 桥接模式（降低延迟）
2. 提高回报率到 500Hz 或 750Hz
3. 检查 WiFi 信号强度

---

## 八、架构原理

```
┌──── 手机 Android ────┐     ┌──── 电脑 Windows ────┐
│                       │     │                       │
│ BluetoothHidDevice   │     │ Windows HID 驱动      │
│  注册为 HID 设备       │────▶│  即插即用             │
│  发送 HID Report      │     │                       │
│                       │     │                       │
│ UdpBridge             │     │ receiver.exe (C)      │
│  250Hz 发送 24 字节包  │────▶│  解析 → ViGEmBus      │
│  等待 4 字节 ACK       │◀────│  → 虚拟 Xbox 360     │
│                       │     │                       │
└───────────────────────┘     └───────────────────────┘
```

**蓝牙 HID**：电脑识别手机为标准 HID 设备，不需要任何驱动
**WiFi UDP**：高速数据通道，低延迟手柄传输
**ViGEmBus**：Windows 虚拟手柄驱动，让电脑识别为 Xbox 360 手柄

---

## 九、文件清单

### 手机端（Android）

| 文件 | 说明 |
|------|------|
| `app/build/outputs/apk/debug/app-debug.apk` | 编译产物，安装到手机 |
| `app/src/main/assets/installer/receiver.exe` | PC 端接收器，随 APK 打包 |

### PC 端（C 项目）

| 文件 | 说明 |
|------|------|
| `pc_receiver_c/build/receiver.exe` | 编译产物，PC 运行 |
| `pc_receiver_c/include/vigem-client.h` | ViGEmClient 头文件 |
| `pc_receiver_c/vigem_client.cpp` | 静态链接进 exe |
| `pc_receiver_c/res/ViGEmBus_Setup.exe` | 驱动安装器 |

---

## 十、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v2.0 | 2026-08-06 | WiFi UDP 桥接、C 版 receiver、系统托盘、回报率可调 |
| v1.0 | 2026-07-xx | 蓝牙 HID 基础功能 |

---

## 十一、技术支持

如遇到问题，请提供以下信息：
- 手机型号和 Android 版本
- 电脑 Windows 版本
- 蓝牙适配器型号
- 手机 App 状态截图
- PC 端事件查看器错误日志
