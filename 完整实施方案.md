# 完整实施方案（蓝牙 HID + WiFi UDP 桥接）

> 整理时间：2026-08-06
> 状态：Android 端 ✅ 已完成 | PC 端 C 程序 🔧 待完善 RFCOMM + IP 发现

---

## 一、整体架构

```
┌─────────────────── 手机 Android ───────────────────┐     ┌─────────────────── 电脑 Windows ───────────────────┐
│                                                     │     │                                                     │
│  ① BluetoothHidDevice                               │     │  Windows 内置 HID 驱动                              │
│     注册为 HID 设备（键盘+鼠标+手柄组合）              │────▶│  即插即用，无需任何安装                               │
│     发送 HID Report（11 字节手柄）                    │     │                                                     │
│                                                     │     │                                                     │
│  ② WifiCommandBridge (RFCOMM 服务端)                 │     │  receiver.exe (C)                                    │
│     BluetoothServerSocket 监听                        │◀────│     ③ RFCOMM 客户端连接                               │
│     文本行协议：JSON 命令 ←── 状态机                   │     │     解析命令 / 回传 IP / 心跳                          │
│                                                     │     │                                                     │
│  ③ UdpBridge                                        │     │  receiver.exe (C)                                    │
│     250Hz 发送 24 字节手柄包                          │────▶│     ④ UDP 接收 → 解析 → ViGEmBus → 虚拟 Xbox 360       │
│     等待 4 字节 ACK 确认                              │◀────│     每帧回 ACK                                        │
│                                                     │     │                                                     │
└─────────────────────────────────────────────────────┘     └─────────────────────────────────────────────────────┘
```

## 二、协议规范（兼容红线，一字节都不能改）

### 2.1 RFCOMM 命令通道（文本行协议，UTF-8，\n 分隔）

| 方向 | 命令 | 说明 |
|------|------|------|
| 手机→PC | `{"cmd":"wifi_info","ssid":"MyWiFi","ip":"192.168.1.5"}` | WiFi 同网检测 |
| PC→手机 | `{"cmd":"wifi_ok","pc_ip":"192.168.1.100"}` | 同网 + 回传 PC IP |
| PC→手机 | `{"cmd":"wifi_mismatch","my_ssid":"OtherWiFi"}` | 不同网 |
| 手机→PC | `{"cmd":"install_driver"}` | 请求检查/安装驱动 |
| PC→手机 | `{"cmd":"driver_ok"}` | 驱动已就绪 |
| PC→手机 | `{"cmd":"driver_installed_ok"}` | 驱动安装完成 |
| 手机→PC | `{"cmd":"start_udp"}` | 启动 UDP 数据通道 |
| PC→手机 | `{"cmd":"udp_ready"}` | UDP 通道就绪 |
| 手机→PC | `ping` | 心跳（每 2 秒） |
| PC→手机 | `pong` | 心跳应答 |
| 手机→PC | `{"cmd":"set_rate","hz":500}` | 切换回报率 |

### 2.2 UDP 数据包（手机→电脑）

- 端口：**47808**
- 包长：**24 字节**，小端序

| 偏移 | 类型 | 字段 | 说明 |
|------|------|------|------|
| 0 | uint32 | seq | 自增序号 |
| 4 | uint32 | mask | 18 位按键位图 |
| 8 | float | leftX | 左摇杆 X，-1.0 ~ 1.0 |
| 12 | float | leftY | 左摇杆 Y（Android 上推为 -1） |
| 16 | float | rightX | 右摇杆 X |
| 20 | float | rightY | 右摇杆 Y |

### 2.3 按键位图（mask）

| bit | 含义 | XInput 映射 |
|-----|------|-------------|
| 0 | A | 0x1000 |
| 1 | B | 0x2000 |
| 2 | X | 0x4000 |
| 3 | Y | 0x8000 |
| 4 | LB | 0x0100 |
| 5 | RB | 0x0200 |
| 6 | LT | 模拟扳机（按=255） |
| 7 | RT | 模拟扳机（按=255） |
| 8 | Select/Back | 0x0020 |
| 9 | Start | 0x0010 |
| 10 | L3 | 0x0040 |
| 11 | R3 | 0x0080 |
| 12 | DPadUp | 0x0001 |
| 13 | DPadDown | 0x0002 |
| 14 | DPadLeft | 0x0004 |
| 15 | DPadRight | 0x0008 |
| 16 | C | 忽略 |
| 17 | Z | 忽略 |

### 2.4 ACK（电脑→手机）

- 4 字节，小端 uint32 = 最近一包的 seq

### 2.5 摇杆转换

- float -1.0~1.0 → short -32768~32767
- Y 轴取反：`sThumbY = -toAxis(y)`（Android 上推 -1，XInput 上推 +）

### 2.6 卡键保护

- 超过 2 秒未收到数据 → 释放所有按键回中

## 三、回报率档位

| 档位 | 回报率 | 间隔 | 场景 |
|------|--------|------|------|
| 省电 | 125Hz | 8ms | 蓝牙默认 |
| 均衡 | 250Hz | 4ms | 日常 |
| 电竞 | 500Hz | 2ms | WiFi 默认 |
| 极致 | 750Hz | ~1.3ms | 电竞 |

## 四、手机端（Android）已完成

### 已实现的文件

| 文件 | 说明 |
|------|------|
| `wifi/WifiCommandBridge.kt` | RFCOMM 通道管理（服务端监听、收发） |
| `wifi/WifiCommandHandler.kt` | 命令解析 + 状态机 + 心跳 + 自动发现 PC IP |
| `input/UdpBridge.kt` | UDP 发送（可变回报率、ACK 等待） |
| `input/InputBridge.kt` | HID 手柄输入桥接（可变回报率） |
| `ui/HidScreen.kt` | WiFi 桥接卡片 + 回报率滑块 + 状态显示 |
| `MainActivity.kt` | 整合所有组件 |

### 状态机流程

```
Idle → WaitingWifi → WifiOk(pcIp) → Ready → UDP 数据通道运行
                  ↘ WifiMismatch → 提示用户切换 WiFi
```

### RFCOMM 自动 IP 发现

1. 手机发 `wifi_info` → PC 回 `wifi_ok {pc_ip:"192.168.x.x"}`
2. 手机自动解析 `pc_ip` → 填入 UdpBridge
3. 用户无需手动输入 IP

## 五、PC 端（C receiver.exe）待实现

### 5.1 已完成

- ✅ 基础 UDP 接收 + ViGEmBus 虚拟手柄
- ✅ 24 字节包解析 + ACK 回传
- ✅ 按键位图 → XInput 映射
- ✅ 摇杆转换 + Y 轴取反
- ✅ 卡键保护（2 秒无数据释放按键）

### 5.2 待实现（RFCOMM 命令通道）

需要在 `receiver.c` 中新增以下功能：

#### A. 获取本机 WiFi IP（用于回传给手机）

```c
// 方法：通过 UDP socket 推断出口 IP（最简单，零额外依赖）
char* get_local_ip(void) {
    static char ip_str[64];
    SOCKET sock = socket(AF_INET, SOCK_DGRAM, 0);
    if (sock == INVALID_SOCKET) return NULL;
    
    // 连到公共 DNS，不需要真正发包
    sockaddr_in dest = {0};
    dest.sin_family = AF_INET;
    dest.sin_port = htons(53);
    inet_pton(AF_INET, "8.8.8.8", &dest.sin_addr);
    connect(sock, (sockaddr*)&dest, sizeof(dest));
    
    sockaddr_in local = {0};
    int len = sizeof(local);
    getsockname(sock, (sockaddr*)&local, &len);
    closesocket(sock);
    
    inet_ntop(AF_INET, &local.sin_addr, ip_str, sizeof(ip_str));
    return ip_str;
}
```

#### B. RFCOMM 客户端连接（可选增强）

```c
// RFCOMM 客户端连接到手机
SOCKET rfcomm_connect(bt_addr_t phone_addr) {
    SOCKET s = socket(AF_BTH, SOCK_STREAM, BTHPROTO_RFCOMM);
    SOCKADDR_BTH addr = {0};
    addr.btAddr = phone_addr;
    addr.serviceClassId = RFCOMM_SPP_UUID;
    addr.port = channel;  // SDP 查询获得
    connect(s, (SOCKADDR*)&addr, sizeof(addr));
    return s;
}
```

#### C. 命令处理逻辑

```c
void handle_rfcomm_command(const char* line) {
    // 解析 JSON 命令...
    if (strstr(line, "\"wifi_info\"")) {
        char* my_ip = get_local_ip();
        // 对比 SSID...
        sprintf(buf, "{\"cmd\":\"wifi_ok\",\"pc_ip\":\"%s\"}\n", my_ip);
        rfcomm_send(buf);
    }
    // 处理其他命令...
}
```

#### D. 后台运行 + 系统托盘

```c
// 隐藏控制台
ShowWindow(GetConsoleWindow(), SW_HIDE);

// 托盘图标
NOTIFYICONDATAW nid = {0};
// ... 初始化托盘 ...
Shell_NotifyIconW(NIM_ADD, &nid);

// 开机自启（注册表）
RegSetValueExW(hKey, L"HidReceiver", 0, REG_SZ, ...);
```

### 5.3 PC 端文件结构

```
pc_receiver_c/
├── build.bat
├── include/
│   └── vigem-client.h       # ViGEmClient API
├── vigem_client.cpp        # 静态链接进 exe
├── res/
│   └── ViGEmBus_Setup.exe   # 驱动安装器（嵌入资源）
└── src/
    ├── main.c               # 入口 + 命令行参数
    ├── receiver.h           # 公共类型声明
    ├── protocol.c           # 24 字节包解析
    ├── vigem_wrapper.c      # ViGEmClient 封装
    ├── udp_receiver.c       # UDP 主循环 + ACK + 卡键保护
    ├── button_mapper.c      # 按键映射
    ├── installer.c          # --install/--uninstall（UAC+驱动+自启+托盘）
    ├── ip_discovery.c       # ★ 新增：获取本机 WiFi IP
    └── rfcomm_channel.c     # ★ 新增：RFCOMM 客户端 + 命令处理
```

### 5.4 构建命令

```batch
cl /O2 /MT /W3 ^
   /D_WIN32_WINNT=0x0A00 ^
   /Fe:build\receiver.exe ^
   src\main.c ^
   src\protocol.c ^
   src\vigem_wrapper.c ^
   src\udp_receiver.c ^
   src\button_mapper.c ^
   src\installer.c ^
   src\ip_discovery.c ^
   src\rfcomm_channel.c ^
   vigem_client.cpp ^
   /I include ^
   ws2_32.lib ole32.lib shell32.lib advapi32.lib wlanapi.lib
```

## 六、用户使用流程

### 首次设置（一次性）

```
1. 手机通过蓝牙推送 receiver.exe 到电脑
   ↓
2. 电脑双击 receiver.exe --install
   → 安装 ViGEmBus 驱动（提权一次）
   → 注册开机自启
   → 托盘图标出现
   ↓
3. 以后开机自动运行，无需操作
```

### 日常使用

```
1. 手机 + 电脑蓝牙配对
   ↓
2. 电脑同 WiFi
   ↓
3. 手机打开 App → 自动 RFCOMM 连接
   → 自动发现 PC IP
   → 自动启动 UDP 通道
   → 手柄数据通过 WiFi 低延迟传输
   ↓
4. 关闭 App → 自动回退蓝牙 HID
```

## 七、体积对比

| 方案 | 体积 | 运行时依赖 |
|------|------|-----------|
| Python + PyInstaller | 50~100 MB | Python 运行时 |
| Java + JRE | 80~150 MB | JRE |
| **C + 静态链接** | **1~5 MB** | **零依赖** |

## 八、待办清单

- [x] 手机端 RFCOMM 命令处理器（WifiCommandHandler.kt）
- [x] 手机端 UDP 桥接（UdpBridge.kt）
- [x] 手机端回报率滑块 UI
- [x] 手机端自动 IP 发现逻辑
- [x] PC 端基础 UDP 接收 + ViGEmBus
- [x] PC 端 24 字节包解析 + ACK
- [x] PC 端按键映射 + 摇杆转换
- [x] PC 端卡键保护
- [ ] PC 端 **RFCOMM 客户端**（连接手机命令通道）
- [ ] PC 端 **WiFi IP 发现**（回传给手机）
- [ ] PC 端 **命令处理**（wifi_info / start_udp 等）
- [ ] PC 端 **系统托盘** + 后台运行
- [ ] PC 端 **开机自启** 注册表
- [ ] PC 端 **资源嵌入**（ViGEmBus_Setup.exe 打入 exe）
- [ ] PC 端 **UTF-8 控制台**（修复中文乱码）
- [ ] 端到端联调测试
